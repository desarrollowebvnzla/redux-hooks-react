import {createAction} from 'redux-actions';

export const hadleApiStart = createAction('hadleApiStart');

export const getProductsSuccess = createAction('getProductsSuccess');

export const handleApiError = createAction('handleApiError');
